# frozen_string_literal: true

class Placeholder < ActiveRecord::Migration[8.0]
  def change
    # Placeholder migration so `db:migrate` matches schema.rb.
    # Replace with real tables (draws, bank_accounts, etc.) in a follow-up.
  end
end
